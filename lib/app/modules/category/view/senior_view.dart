import 'package:flutter/material.dart';

class SeniorView extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Seniors Care'),),
      body: Center(
        child: Text('Development under process'),
      ),
    );
  }
}
