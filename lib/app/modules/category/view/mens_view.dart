import 'package:flutter/material.dart';

class MensView extends StatelessWidget {
  const MensView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Mens Care'),),
      body: Center(
        child: Text('Development under process'),
      ),
    );
  }
}
