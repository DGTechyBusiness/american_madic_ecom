import 'package:flutter/material.dart';

class WomensView extends StatelessWidget {
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Women\'s Care'),),
      body: Center(
        child: Text('Development under process'),
      ),
    );
  }
}
